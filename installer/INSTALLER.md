# Astrante Theme Installer

Это установщик для темы Astrante Theme с функцией AutoAccept для Pengu Loader.

## 📦 Создание инсталлятора

### Требования

Перед сборкой инсталлятора убедитесь, что у вас установлены:

1. **Node.js** (версия 20 или выше)
   - Скачать: https://nodejs.org/

2. **Inno Setup 6.x**
   - Скачать: https://jrsoftware.org/isdl.php
   - Установить в стандартную папку или добавить `ISCC.exe` в PATH

### Сборка инсталлятора

#### Автоматическая сборка (рекомендуется)

```bash
npm run build:installer
```

Этот скрипт автоматически:
1. Соберёт проект (`npm run build`)
2. Создаст инсталлятор в папке `installer/output/`

#### Ручная сборка

```bash
# 1. Собрать проект
npm run build

# 2. Скомпилировать инсталлятор
ISCC.exe installer/installer.iss
```

## 🚀 Установка для конечных пользователей

1. Запустите `AstranteTheme-Installer.exe`
2. Выберите папку установки Pengu Loader
3. Нажмите "Install"
4. Готово! Тема появится в Pengu Loader

## 📂 Структура файлов инсталлятора

```
installer/
├── installer.iss           # Скрипт инсталлятора
├── build-installer.js      # Скрипт автоматической сборки
├── app_icon.ico            # Иконка инсталлятора (опционально)
└── output/
    └── AstranteTheme-Installer.exe  # Готовый инсталлятор
```

## 🎨 Кастомизация инсталлятора

### Изменение иконки

Добавьте файл `app_icon.ico` в папку `installer/`, или удалите строку `SetupIconFile=app_icon.ico` в [installer/installer.iss](installer/installer.iss:48).

### Изменение изображений wizard'а

Добавьте файлы в папку `installer/`:
- `installer-image.bmp` (164×314 px)
- `installer-small.bmp` (55×55 px)

### Изменение версий

Редактируйте в начале [installer/installer.iss](installer/installer.iss:4-8):

```iss
#define AppName "Astrante Theme"
#define AppVersion "1.0.0"
#define AppPublisher "Astrante"
```

### Добавление дополнительных файлов

Добавьте в секцию [Files] в [installer/installer.iss](installer/installer.iss:82):

```iss
[Files]
Source: "..\dist\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs
Source: "..\README.md"; DestDir: "{app}"; Flags: ignoreversion
```

## 🔧 Устранение проблем

### Ошибка "Inno Setup not found"

- Установите Inno Setup: https://jrsoftware.org/isdl.php
- Перезапустите терминал после установки
- Убедитесь, что `ISCC.exe` в PATH

### Ошибка "Build output folder dist not found"

- Сначала запустите `npm run build`
- Убедитесь, что сборка прошла успешно

### Инсталлятор не находит Pengu Loader

- Пользователь может выбрать любую папку вручную
- Автоопределение ищет в стандартных locations:
  - `C:\Program Files\Pengu Loader`
  - `C:\Program Files (x86)\Pengu Loader`
  - `%LOCALAPPDATA%\Programs\Pengu Loader`

## 📝 Лицензия

MIT License - см. [LICENSE](../LICENSE) для подробностей
